/**
 * 
 * @returns 
 * Simulation of UI for main menu and toolbar of Visual Studio for small screens.
 * Currently only for decoration purposses.
 */
export default function DecorativeTopComponentMob(){
    return(
        <div className="decorative-top-mobile">
            <span className="text-primary">MarinaDotNet.Portfolio</span>
        </div>
    );
}